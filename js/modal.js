function openModal(url) {
  const modal = document.getElementById(url);
  modal.style.display = "flex";
}

function closeModal(url) {
  const modal = document.getElementById(url);
  modal.style.display = "none";
}
